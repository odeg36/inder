{% if admin.hasAccess('create', object) and admin.hasRoute('edit') %}
    <a href="{{ path("admin_logic_oferta_create", {estrategia: object.id}) }}" class="btn btn-sm btn-default edit_link" 
       title="{{ 'btn.accion.crear'|trans }}">
        <i class="fa fa-plus-circle" aria-hidden="true"></i>
        {{ 'btn.crear.oferta'|trans }}
    </a>
{% endif %}
